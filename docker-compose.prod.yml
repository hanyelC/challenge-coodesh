version: '3.8'

services:
  db:
    image: postgres
    container_name: postgres_db
    restart: always
    ports:
      - 15432:5432
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
